{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/moranbickel/russian-judge/blob/master/schemas/verdict.schema.json",
  "title": "Russian Judge Verdict",
  "description": "Schema for a Russian Judge verdict produced by an adversarial reviewer. Mirrors the JSON form documented in templates/verdict-template.md and the field rules in PROTOCOL.md §7. The five required fields are the core verdict; the optional `record` block (PROTOCOL.md §13) carries the auditable-record metadata that programmatic pipelines use to chain, bind, and attribute verdicts.",
  "type": "object",
  "required": ["round", "modality", "score", "findings", "verdict"],
  "additionalProperties": false,
  "properties": {
    "round": {
      "type": "string",
      "pattern": "^R[0-9]+$",
      "description": "Review round identifier. R1, R2, R3, etc."
    },
    "modality": {
      "type": "string",
      "enum": ["code", "domain", "dual"],
      "description": "Review modality. Per PROTOCOL.md §9. Dual indicates two independent verdicts (one Code, one Domain) rolled up; in practice the two are usually emitted as separate verdicts, each with its own modality."
    },
    "score": {
      "type": "number",
      "minimum": 0,
      "maximum": 10,
      "multipleOf": 0.1,
      "description": "Reviewer score, [0.0, 10.0], one decimal of precision. Anchored bands per PROTOCOL.md §4.1."
    },
    "findings": {
      "type": "object",
      "required": ["critical", "important", "minor"],
      "additionalProperties": false,
      "properties": {
        "critical": {
          "type": "array",
          "items": { "$ref": "#/$defs/findingCI" },
          "description": "Critical findings. Block shipping unconditionally."
        },
        "important": {
          "type": "array",
          "items": { "$ref": "#/$defs/findingCI" },
          "description": "Important findings. Block shipping at the pass floor."
        },
        "minor": {
          "type": "array",
          "items": { "$ref": "#/$defs/findingM" },
          "description": "Minor findings. Do not block shipping."
        },
        "notes": {
          "type": "array",
          "items": { "$ref": "#/$defs/findingNote" },
          "description": "Optional. Notes are observations with no score or floor impact: out-of-scope remarks, no-action recommendations, sibling-class observations, future-improvement framing. Per PROTOCOL.md §3.6, a finding whose own narrative says 'no fix required here' or 'out of scope' belongs in Notes, not Minor. Notes do not count toward the floor."
        }
      }
    },
    "verdict": {
      "type": "string",
      "enum": ["PASS", "REVISE", "REWORK"],
      "description": "Computed verdict value. PASS requires score>=9.0 AND 0 critical AND 0 important. REVISE is the default below the floor. REWORK is reserved for score<7.0. Validators should verify the verdict is consistent with the score and finding counts."
    },
    "record": {
      "type": "object",
      "description": "Optional auditable-record metadata, per PROTOCOL.md §13. Present when the verdict is stored as a durable, attributable, chainable record (audit trail, programmatic pipeline) rather than an ephemeral message. Omit for ad-hoc single-session use.",
      "additionalProperties": true,
      "properties": {
        "verdict_id": {
          "type": "string",
          "description": "Stable identifier for this verdict, unique within the verdict store (e.g. a UUID). Referenced by `predecessor_verdict` to chain rounds."
        },
        "predecessor_verdict": {
          "type": ["string", "null"],
          "description": "The `verdict_id` of the preceding round's verdict, or null for R1. Chains the round protocol (PROTOCOL.md §5) into a verifiable lineage rather than relying on round numbers alone."
        },
        "review_scope": {
          "type": "object",
          "description": "What the reviewer actually examined, per modality. Makes a PASS honest: it certifies only this scope, not adjacent files or downstream consumers (PROTOCOL.md §4.2.1). At least one dimension list is present; the off-modality list is an explicit empty array, never omitted.",
          "additionalProperties": false,
          "properties": {
            "code_dimensions": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Code axes reviewed (e.g. 'edge cases', 'contract conformance'). Empty array for a domain-only review."
            },
            "domain_dimensions": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Domain/content axes reviewed (e.g. 'citation accuracy', 'internal consistency'). Empty array for a code-only review."
            }
          }
        },
        "covers_range": {
          "type": "string",
          "description": "The exact change the verdict is bound to (e.g. a commit range `<base>..<head>`, a revision id, or a content hash). A verdict authored against one state does not certify a later state; binding the verdict to a range is what lets tooling gate the *shipped* change rather than a stale one."
        },
        "reviewer_model": {
          "type": "string",
          "description": "Identifier of the model (or human) that produced this verdict. Required for cross-model dispatch (PROTOCOL.md §9.4) to be auditable — you cannot tell which model caught a finding without recording which model reviewed."
        },
        "review_round": {
          "type": "string",
          "pattern": "^R[0-9]+$",
          "description": "Round identifier, mirroring the top-level `round`. Carried in the record so the stored verdict is self-describing."
        },
        "pass_asserted": {
          "type": "boolean",
          "description": "The reviewer's own PASS assertion. The floor invariant (PROTOCOL.md §4.4) requires this to equal `(score >= 9.0) AND (critical + important + minor == 0)`. A `pass_asserted: true` with any non-zero blocking-class count is a malformed verdict; validators recompute and reject the mismatch."
        },
        "issued_at": {
          "type": "string",
          "description": "Timestamp the verdict was issued (ISO 8601 UTC recommended). Orders the round lineage and supports drift auditing (PROTOCOL.md §10.2)."
        }
      }
    }
  },
  "$defs": {
    "findingCI": {
      "type": "object",
      "required": ["id", "description", "impact"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[CI]-[1-9][0-9]*$",
          "description": "Finding identifier. Class letter (C or I) + dash + 1-indexed integer, monotonically increasing within the class."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "One-sentence description of the defect, ending with a period."
        },
        "impact": {
          "type": "string",
          "minLength": 1,
          "description": "One-sentence description of the consequence if shipped, ending with a period. Required for Critical and Important."
        }
      }
    },
    "findingM": {
      "type": "object",
      "required": ["id", "description"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^M-[1-9][0-9]*$",
          "description": "Minor finding identifier. M-1, M-2, etc."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "One-sentence description of the defect, ending with a period."
        },
        "impact": {
          "type": "string",
          "description": "Optional one-sentence consequence statement. Not required for Minor."
        }
      }
    },
    "findingNote": {
      "type": "object",
      "required": ["id", "description"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^N-[1-9][0-9]*$",
          "description": "Note identifier. N-1, N-2, etc."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "One-sentence observation. Notes carry no score or floor impact (PROTOCOL.md §3.6)."
        }
      }
    }
  }
}
